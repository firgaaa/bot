# ============================================================
# Fichier de configuration centralisé - BOT KFC
# ============================================================
# Copier ce fichier en value.env et remplir les valeurs réelles.
# Ne jamais commiter value.env (déjà dans .gitignore).
# ============================================================

# === Bot Telegram ===
BOT_TOKEN=
ADMIN_ID=0
MODERATOR_ID=0
SELLER_ID=0

# === Base de données PostgreSQL (partagée bot + API) ===
DATABASE_URL=postgres://user:password@localhost:5432/bot_db
DB_HOST=localhost
DB_PORT=5432
DB_NAME=bot_db
DB_USER=postgres
DB_PASSWORD=postgres

# === API REST (auth Basic pour /insert, /generate, etc.) ===
BASIC_AUTH_USER=
BASIC_AUTH_PASSWORD=

# === API KFC Cartes (utilisée par le bot pour appeler l'API locale) ===
KFC_API_URL=http://localhost:8080
KFC_API_USERNAME=
KFC_API_PASSWORD=
KFC_STORAGE_TABLE=kfc_storage

# === API Rust (serveur) ===
PORT=8080
RUST_LOG=info

# === Optionnel : Token pour dérivation des tokens revendeurs ===
# TOKEN_PUBLIC_SECRET=

# ============================================================
# Config initiale (utilisée UNIQUEMENT à la création des tables
# par start.py ; ensuite le bot lit tout depuis la table config)
# ============================================================
CONFIG_POINT_MIN=150
CONFIG_POINT_MAX=2500
CONFIG_CARD_MARGIN=300
CONFIG_PAYMENT_URL=https://example.com/pay
CONFIG_STAFF_CHANNEL_ID=
CONFIG_STAFF_THREAD_PAYMENT=
CONFIG_STAFF_THREAD_REPORT=
CONFIG_STAFF_THREAD_ENTRETIEN=
CONFIG_EMERGENCY_STOP=false
CONFIG_ANNOUNCEMENT_TEXT=Aucune annonce pour le moment.
CONFIG_ANNOUNCEMENT_PHOTO=
